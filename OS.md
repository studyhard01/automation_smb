# OS.md — Overall Structure (전체 시스템 구조)

> 이 문서는 `automation_smb`가 속한 더 큰 서비스의 **전체 구조와 목표**를 설명한다. 구현 세부는 각 모듈의 코드/README에, 작업 규칙은 [`CLAUDE.md`](./CLAUDE.md)에 둔다.

## 1. 목적과 비전

**최종 목표:** 개발을 모르는 **검사실(분자유전팀) 일반 사용자**가 직접 **자동화 파이프라인을 구성**할 수 있는 **노코딩 툴**.

- 사용자는 코드/스크립트 대신 **자연어(텍스트·음성)** 와 시각적 블록 조합으로 "이런 일을 자동으로 해줘"를 정의한다.
- **`공유 폴더 찾기` 서비스는 이 비전의 첫 성공 케이스(showcase)** 다. "○○ 검사 폴더 찾아줘"라고 텍스트로 치거나 말하면 즉시 해당 공유폴더를 찾아준다. 여기서 검증된 패턴(자연어 명령 → 도구 실행 → 결과)을 다른 검사실 업무로 확장한다.
- **모든 것이 온프레미스/로컬에서 동작한다.** 환자·검체 데이터를 다루므로 외부 클라우드 API(LLM, 음성인식 등)에 의존하지 않는다. 이것이 가장 강한 설계 제약이다. ([6장](#6-보안-모델--데이터-경계) 참고)

## 2. 설계 원칙

1. **Local-first / 온프레미스 전용** — LLM 추론, 음성 인식(STT), 임베딩, 데이터 저장 전부 사내 서버 안에서 완결. 외부로 나가는 네트워크 호출은 기본적으로 없음. ([CLAUDE.md 보안 규칙](./CLAUDE.md))
2. **자연어가 1차 인터페이스** — 텍스트와 음성을 같은 명령 경로로 수렴시킨다(음성 → STT → 텍스트 → 동일 처리). 비개발자가 학습 없이 쓰는 게 핵심.
3. **도구(Tool) 단위 모듈화** — `공유폴더 찾기` 같은 개별 기능은 독립 서비스/도구로 만들고, 오케스트레이터가 이들을 조합한다. 새 기능 추가 = 새 도구 등록.
4. **작성(authoring)과 실행(execution)을 분리** — 사용자의 고통은 *작성*에 있으므로 자연어 작성층은 **직접 만든다(자체 얇은 디스패처)**. 큐·재시도·스케줄 같은 *실행* 인프라는 commodity이므로 밑바닥부터 만들지 않는다. 빌더 UI(n8n/Dify 캔버스)를 사용자 정면에 두지 않는다 — 검사실 직원은 캔버스를 만지지 않는다.
5. **기존 `automation` 스택 위에 얹는다** — 바퀴를 재발명하지 않는다. ([7장 매핑](#7-기존-automation-인프라와의-매핑) 참고)

## 3. 전체 구조 (레이어)+

```
┌──────────────────────────────────────────────────────────────────┐
│  L1. 인터랙션 (사용자 입구)                                          │
│      텍스트 입력 · 음성 입력(마이크) · 결과 표시(화면/음성)             │
└───────────────┬──────────────────────────────────────────────────┘
                │  음성은 로컬 STT로 텍스트화 → 이하 동일 경로
                ▼
┌──────────────────────────────────────────────────────────────────┐
│  L2. 오케스트레이션 (자연어 → 의도 → 라우팅)                          │
│      로컬 LLM이 명령을 해석해: ① 어떤 도구/파이프라인을 ② 어떤         │
│      파라미터로 실행할지 결정 (intent + slot 추출)                    │
└───────────────┬──────────────────────────────────────────────────┘
                ▼
┌──────────────────────────────────────────────────────────────────┐
│  L3. 오케스트레이션/실행                                             │
│      ① 자체 얇은 디스패처(기본) — 단발·선형 체인을 직접 실행          │
│      ② Dify(백엔드, 숨김) — 다단계 LLM 워크플로                       │
│      ③ n8n(기존 유지) — 스케줄·파일순회 등 비-LLM 잡                  │
│      단발 명령이면 L2가 ①로 바로 L4를 부른다                          │
└───────────────┬──────────────────────────────────────────────────┘
                ▼
┌──────────────────────────────────────────────────────────────────┐
│  L4. 도구 / 서비스 (실제 일을 하는 단위)                              │
│      ▣ 공유폴더 찾기(automation_smb)  ▣ 문서 변환  ▣ RAG 검색         │
│      ▣ ClinVar 분류  ▣ Sanger 분석  ▣ … (확장)                      │
│      각 도구는 독립 프로세스/API. OpenAPI로 L2·L3에 등록.             │
└───────────────┬──────────────────────────────────────────────────┘
                ▼
┌──────────────────────────────────────────────────────────────────┐
│  L5. 데이터 / 인프라 (전부 온프레미스)                                │
│      SMB 공유폴더 · Qdrant(벡터) · PostgreSQL · 로컬 LLM/STT/임베딩  │
└──────────────────────────────────────────────────────────────────┘
```

## 4. 레이어별 상세

### L1. 인터랙션
- **텍스트**와 **음성**을 받는 단일 진입점. 음성은 마이크 입력 → **로컬 STT**로 텍스트화한 뒤 텍스트와 완전히 같은 경로로 처리한다(분기 최소화).
- STT는 **외부 음성 API 금지** 제약 때문에 로컬 모델로 구동한다. 후보: `whisper.cpp` 또는 `faster-whisper`(CPU 구동 가능, 한국어 지원). 모델 가중치는 온프레미스에 보관. *(확정 전 — [8장](#8-열린-결정--다음-단계))*
- (선택) 결과를 음성으로 돌려주려면 로컬 TTS를 같은 원칙으로 둔다.

### L2. 오케스트레이션 (자연어 → 실행 결정)
- **로컬 LLM**(`llama.cpp`, OpenAI 호환 엔드포인트)이 사용자 명령을 해석해 **어떤 도구를 어떤 파라미터로** 실행할지 정한다. 예: "지난달 ○○검사 결과 폴더 찾아줘" → `find_share_folder(query="○○검사", period="last_month")`.
- 구현은 **tool/function calling** 패턴 또는 Dify의 도구 라우팅을 사용. 도구 메타데이터(이름·설명·파라미터)는 각 도구의 OpenAPI에서 가져온다(기존 `automation`의 Dify Custom Tool export 방식 재사용).
- **OpenAI 호환 인터페이스로 짜서** 온프레미스 LLM ↔ (불가피할 때) 외부 LLM을 `base_url`만 바꿔 전환 가능하게 한다. 단, 운영 기본값은 항상 로컬.

### L3. 오케스트레이션 / 실행
"노코딩 툴"의 본체는 **빌더 캔버스가 아니라 자연어**다. 사용자는 도구를 직접 엮지 않고, L2가 해석한 결과를 이 계층이 실행한다. 세 갈래로 분담한다(우선순위 순):

1. **자체 얇은 디스패처 (기본·우선)** — LLM function-calling으로 도구(L4, OpenAPI)를 골라 파라미터를 채워 호출. **단발 명령과 간단한 선형 체인**("찾고 → 변환하고 → 알림")까지 직접 처리. 검사실 자동화의 대부분이 이 범위이며, 이것이 "자체 OS"의 씨앗이다. 온프레미스·감사 가능성 측면에서 가장 통제하기 쉽다.
2. **Dify (다단계 LLM 워크플로의 숨은 백엔드)** — 분기·반복·상태가 얽힌 **LLM 추론 중심** 워크플로는 Dify에 위임. 이미 온프레미스에 운영 중이고, L4 도구가 Custom Tool(OpenAPI)로 등록되는 경로가 구축돼 있어(`automation`의 `export_dify_tools.py`) 통합 비용이 낮다. **단, Dify를 사용자 정면에 두지 않는다** — 정면은 항상 L1/L2(자체 작성층).
3. **n8n (기존 유지, 역할 축소)** — 이미 "검사실 AI" 워크플로(SMB 재귀 스캔, BFS Iterator)를 돌리는 **스케줄·파일순회·커넥터 중심 비-LLM 잡** 영역만 담당. 새 복잡 워크플로는 n8n이 아니라 Dify로 보낸다.

> **분담 기준:** 단발/선형 → ① 자체 디스패처 · LLM 추론 중심 다단계 → ② Dify · 스케줄/파일순회/커넥터 중심 → ③ n8n.
> **겹침 주의:** Dify도 의도 라우팅을 할 수 있어 L2와 역할이 겹칠 수 있다. **L2 = 사용자 입구 + 단발/선형 디스패치**, **Dify = 다단계 LLM 워크플로 실행 백엔드**로 경계를 고정한다. 오케스트레이션 브레인을 둘로 만들지 않는다.

### L4. 도구 / 서비스
- 각 기능은 **독립 서비스(자체 API·포트)** 로 만들고 OpenAPI로 등록 → L2/L3이 호출. 기능이 기존 코드와 ~80% 이상 독립적이면 별도 모듈로 분리(작업공간 공통 원칙).
- **첫 도구 = `automation_smb`의 공유폴더 찾기.** 이후 문서 변환·RAG 검색·ClinVar·Sanger 등이 같은 방식으로 도구 목록에 합류한다.

### L5. 데이터 / 인프라
- **SMB 공유폴더**(검체·검사 파일, read-only 기본), **Qdrant**(벡터 검색), **PostgreSQL**, 그리고 **로컬 LLM·STT·임베딩 서버**. 전부 사내망 안.

## 5. 성공 케이스: 공유 폴더 찾기 (end-to-end)

```
사용자: (음성) "OO검사 결과 폴더 찾아줘"
   │
   ├─ L1  마이크 입력 → 로컬 STT → "OO검사 결과 폴더 찾아줘" (텍스트)
   │
   ├─ L2  로컬 LLM 의도 해석 → find_share_folder(query="OO검사 결과")
   │
   ├─ L4  automation_smb: SMB 공유폴더 탐색(인덱스/순회) → 후보 경로 N건
   │        (자격증명은 .env, read-only 접근)
   │
   └─ L1  결과 표시: 경로 목록 / 바로 열기 (필요 시 음성 안내)
```

- **텍스트 명령**은 L1의 STT 단계만 건너뛰고 동일 경로(L2→L4)를 탄다 — 그래서 두 입력이 하나의 처리 파이프라인으로 수렴한다.
- 탐색 구현·SMB 연결 패턴은 [`CLAUDE.md`의 "참고 구현"](./CLAUDE.md) 절(=`../automation`의 `smb_converter`/`smb_raw`, BFS Iterator)을 재사용한다.

## 6. 보안 모델 — 데이터 경계

핵심 불변식: **환자·검체 데이터와 사내 자격증명은 온프레미스 경계를 넘지 않는다.**

| 항목 | 원칙 |
|---|---|
| LLM 추론 | 로컬 `llama.cpp`. 외부 LLM API 기본 사용 안 함 |
| 음성 인식(STT) | 로컬 모델(whisper.cpp/faster-whisper). 클라우드 음성 API 금지 |
| 임베딩/검색 | 로컬 임베딩 + 온프레미스 Qdrant |
| 공유폴더 | read-only 기본, 자격증명은 `.env`(=`.gitignore`), 평문 노출 금지 |
| 외부 전송 | 파일 내용·경로·목록을 외부로 보내지 않음. 불가피 시 사용자에게 사전 확인 |
| 배포/푸시 | `git push`·원격 배포는 명시 승인 후 |

상세 운영 규칙은 [`CLAUDE.md`의 "회사 보안"](./CLAUDE.md) 절을 따른다.

## 7. 기존 `automation` 인프라와의 매핑

이 서비스는 대부분 `../automation`에 이미 떠 있는 컴포넌트를 재사용한다(신규 개발 최소화).

| 레이어 | 신규/재사용 | 기존 자산 |
|---|---|---|
| L2 로컬 LLM | 재사용 | `llama.cpp`(Qwen 계열, OpenAI 호환), `llm/client.py` |
| L2 도구 등록 | 재사용 | Dify Custom Tool / OpenAPI export (`scripts/export_dify_tools.py`) |
| L3 단발·선형 실행 | **신규(자체)** | 얇은 디스패처 — LLM function-calling over OpenAPI 도구 |
| L3 다단계 LLM 워크플로 | 재사용 | **Dify**(온프레미스 운영 중, Custom Tool 통합 완료) |
| L3 스케줄·비-LLM 잡 | 재사용 | n8n("검사실 AI" SMB 재귀 스캔 등) |
| L4 공유폴더 찾기 | **신규(본 repo)** | `automation`의 `smb_converter`/`smb_raw` 패턴 차용 |
| L4 기타 도구 | 재사용 | RAG, ClinVar, Sanger API |
| L5 데이터 | 재사용 | SMB, Qdrant, PostgreSQL |
| **L1 음성(STT)** | **신규(미구축)** | 작업공간에 아직 없음 — 본 비전에서 새로 도입 |

## 8. 열린 결정 / 다음 단계

**결정됨**
- **오케스트레이션 전략** — 정면은 *자체 얇은 디스패처*(단발/선형). 다단계 LLM 워크플로는 *Dify* 백엔드에 위임(사용자 정면 노출 X). 스케줄/비-LLM 잡은 *n8n* 유지. ([L3 상세](#l3-오케스트레이션--실행))
- **첫 수직 슬라이스** — *텍스트 명령 → 공유폴더 찾기*. 음성(STT)·다단계 워크플로는 그 다음.

**아직 미확정 (임의 확정 금지)**
- **STT 엔진/모델** — `whisper.cpp` vs `faster-whisper`, 모델 크기(정확도↔CPU 비용), 한국어+검사 용어 처리.
- **공유폴더 탐색 전략** — 사전 인덱싱(빠름, 갱신 필요) vs 실시간 순회(최신, 느림). 데이터 규모로 판단.
- **자체 디스패처 → Dify 승격 기준** — 어느 복잡도부터 Dify로 넘길지의 구체 임계선(분기 수, 상태 유지 여부 등).

권장 출발점: **공유폴더 찾기 도구(L4) + 텍스트 명령(L1/L2) + 자체 얇은 디스패처(L3①)** 의 수직 슬라이스를 먼저 완성해 "자연어 → 도구 실행 → 결과"를 증명하고, 그 위에 **음성(STT)** 과 **다단계 워크플로(L3② Dify)** 를 순차로 얹는다.
