# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## 이 프로젝트

`automation_smb` — **공유 폴더(SMB) 접근 및 자동 탐색 서비스**. 인덱스 검색 API와 tool 기반 챗봇 Playground가
구현된 초기 기능 검증 단계다. **아직 모르는 부분을 임의로 확정해 구현하지 말고** 최소 가정만 두고 진행하면서
그 가정을 드러낸다.

> 도메인 맥락: 진단검사실(분자유전팀)의 **팀내 공유폴더**를 다루는 서비스다. 공유폴더에는 환자 검체·검사 관련 **민감 의료 파일**이 들어있다고 전제한다. 운영은 **온프레미스(사내망)** 기준.

## 현재 개발 단계 — 합성 데이터 전용 챗봇 테스트

현재 저장소는 **실제 의료 환경과 무관한 합성·더미 데이터만 사용하는 기능 테스트 환경**이다. 이 단계의 목표는
챗봇 대화, tool 선택·호출, 검색 결과 반환, trace와 지연 측정이 막힘없이 동작하는지 확인하는 것이다.

- 의료데이터·개인정보 입력을 추정해 차단하는 검사, provider별 합성 데이터 gate, 별도 안전 프로필처럼 기능 테스트를
  막는 런타임 보안 흐름은 두지 않는다.
- 테스트 fixture, 예시 문구, smoke test 입력은 실제 환자·검사·보고서 형식과 닮지 않은 일반 합성 데이터로 작성한다.
- 기본 기능 테스트가 완료될 때까지 기능 구현과 지연 예산을 우선한다. 운영 전환 보안 검토와 운영용 guardrail은
  별도 단계에서 다시 설계한다.
- 이 선언은 실제 의료데이터 사용 승인이 아니다. 아래 최소 저장소 경계(자격증명 비커밋, 실제 데이터 외부 전송 금지,
  SMB 읽기 전용, 승인 없는 push·배포 금지)는 테스트 단계에도 유지한다.

## 먼저 볼 참고 구현 (바퀴 재발명 금지)

같은 작업공간의 `../automation`에 SMB 접근이 이미 구현돼 있다. 새로 짜기 전에 이걸 읽고 패턴을 재사용한다.

- `../automation/src/automation/smb_converter/smb_client.py` — `smbclient`(smbprotocol) 기반 연결/다운로드 클라이언트. `register_session(host, username, password)`로 세션 등록, `reset_connection_cache()`로 종료.
- `../automation/src/automation/smb_converter/indexer.py` — SMB 순회 → 변환 → 인덱싱 파이프라인.
- `../automation/src/automation/smb_raw/api.py` — 공유폴더 파일 인덱스/접근 API.
- 재귀 전체 스캔은 `automation`의 n8n "검사실 AI" 워크플로가 **BFS Iterator 패턴**으로 푼다(재귀 서브워크플로의 결과 미생성/SQLite 잠금 경합을 피하려 재설계됨). 자동 탐색 로직을 짤 때 이 교훈을 참고한다.
- `../automation/CLAUDE.md` — 작업공간 전반의 환경/규칙 상세.

연결 자격증명 키 이름도 기존 것과 맞춘다: `SMB_HOST`, `SMB_SHARE_NAME`, `SMB_USERNAME`, `SMB_PASSWORD`.

## 테스트 단계에도 유지하는 최소 저장소 경계

현재 기능 테스트에는 합성 데이터만 사용한다. 다만 향후 사내 공유폴더 연결 가능성이 있으므로 아래 저장소·외부 전송
경계는 런타임 입력 차단과 별개로 유지한다.

- **SMB 자격증명·내부 IP·키는 코드/문서/커밋에 절대 넣지 않는다.** `.env`에만 두고 `.env`는 항상 `.gitignore` 대상. 시작하자마자 `.env.example`(값 비움)과 `.gitignore`부터 만든다. 코드/로그에 자격증명이나 비밀번호가 평문으로 보이면 멈추고 사용자에게 알린다.
- **공유폴더의 환자/검사 데이터를 외부로 내보내지 않는다.** 파일 내용·경로·목록을 외부 LLM·검색 API·외부 저장소로 전송·업로드·커밋하지 않는다. 외부 호출이 불가피하면 **무엇이 외부로 나가는지 먼저 사용자에게 확인**받는다.
- **공유폴더는 읽기 기준으로 다룬다.** 명시 요청 없이 공유폴더의 파일을 쓰기/이동/삭제하지 않는다. 탐색·인덱싱은 read-only가 기본.
- **외부 푸시·배포는 명시적 승인 후에만.** `git push`·원격 배포·외부 데이터 전송은 사용자가 분명히 요청했을 때만.
- 운영 경로(`/data/script/fsysy/...`)·내부 IP는 로컬과 다르다. 로컬에서 검증하고 운영 반영은 사용자 확인 후.

## 사내망 환경 함정

개발 머신은 **Windows 11**, 배포 타깃은 **온프레미스 Linux 서버**. 사내망은 **SSL 검사 프록시(self-signed 루트 CA)**를 끼고 있어 외부 접근이 자주 깨진다:

- `pip` SSL 에러 → `pip install <pkg> --trusted-host pypi.org --trusted-host files.pythonhosted.org`
- `uv`에서 `invalid peer certificate: UnknownIssuer` → `uv` 명령에 `--native-tls` 추가
- SMB는 사내망 내부 통신이라 프록시와 무관하지만, **방화벽/포트(445)와 도메인 인증** 이슈가 따로 날 수 있다 — 연결 실패 시 자격증명·공유명·호스트부터 점검.

## 반환 시간(지연) — 테스트 단계의 중요 비기능 요구

사용자가 대화 흐름을 끊지 않고 결과를 받을 수 있도록 지연은 계속 측정·개선한다. 다만 챗봇의 자연어 이해와 답변 품질을
희생하면서까지 최저 지연만 추구하지는 않는다. 자유 형식의 사용자 요청은 LLM/agent가 의미를 해석하는 것을 기본으로 하고,
성능 최적화는 중복 호출 제거, 검색·컨텍스트 축소, 캐시와 연결 재사용을 우선한다.

**목표 예산 (잠정, 측정으로 조정)**
- LLM 없는 로컬 tool 요청 → 결과: **p50 < 1s, p95 < 3s** (인덱스가 떠 있는 상태 기준).
- LLM/RAG 챗봇 요청 → 최종 답변: **p50 < 5s, p95 < 12s**.
- 인메모리 폴더 검색 자체: **< 100ms**.
- (음성 추가 시) STT 포함 end-to-end: **p50 < 8s, p95 < 15s** 목표.

**설계 원칙**
- **인덱스 우선, 실시간 순회 금지(기본).** 매 요청마다 SMB 전체를 도는 건 느려서 못 쓴다. 폴더 트리를 **미리 인덱싱해 메모리/캐시**에 올려두고 **인메모리로 검색**한다. 인덱스 갱신은 백그라운드/주기적으로. 실시간 SMB 순회는 캐시 미스 시 *제한된 범위*의 fallback으로만.
- **모든 외부 호출에 timeout + 시간 예산.** SMB·LLM 호출은 무한정 기다리지 않는다. 시간 예산을 넘기면 구조화된 timeout이나 확보된 근거를 반환하되, 단지 빠르게 보이기 위해 품질이 낮은 추정 답변을 만들지는 않는다.
- **자연어 요청은 LLM/agent 우선.** 자유 형식 채팅을 키워드 규칙으로 분류·응답하는 방식을 기본 최적화로 권장하지 않는다. 규칙 기반 처리는 `/tools`, `/skills` 같은 명시적 명령, 스키마 검증, 확정적인 tool dispatch처럼 의미 해석이 필요 없는 경계에만 사용한다.
- **불필요한 LLM 호출은 제거한다.** 사용자가 UI나 명령으로 tool/skill을 명확히 선택한 경우에는 중복된 의도 판별 호출을 생략할 수 있다. 이는 자연어 답변 자체를 규칙으로 대체하는 것이 아니라 이미 확정된 실행 계획을 바로 수행하는 최적화다.
- **연결·세션은 재사용**(매번 재연결 금지), 응답은 상위 N건만.

**작업 규칙:** 탐색/오케스트레이션 코드를 만지면 응답에 **소요 시간(ms)을 측정·로깅**하고, 예산 초과 원인과
품질·비용·지연의 trade-off를 함께 보고한다. 지연 개선을 이유로 자유 형식 요청을 임의의 키워드 분기로 바꾸지 않으며,
LLM 호출 수, 컨텍스트 크기, 검색 시간과 tool 실행 시간을 각각 측정한 뒤 병목을 최적화한다.

## 도구·관례 (없으면 이걸 기본값으로)

- **Python: `uv` + Python 3.11**, `src/` 레이아웃, 린트 `ruff`(line-length 120), 테스트 `pytest`. 단위/통합 테스트는 `-m "not integration"`으로 분리(실서버 필요한 건 `integration` 마커).
- SMB 라이브러리는 참고 구현과 동일하게 **`smbclient`(smbprotocol)**.
- 입출력 데이터 구조는 **Pydantic 모델**로, 함수엔 type hint + docstring.
- **문서·주석·docstring은 한국어**, **파일명은 영문**(특히 마크다운).

## 빠른 개발 모드 (현재 사용자 요청)

- 현재는 **합성 데이터 전용 챗봇 기능 검증**이 우선이다. 의료데이터 입력 차단이나 별도 보안 gate를 기능 경로에
  다시 추가하지 않는다. 위 최소 저장소 경계만 유지한다.
- 과한 사전 추상화보다 **동작하는 수직 슬라이스 먼저**(연결 → 탐색 → 결과 반환), 리팩토링은 동작 확인 후.
- 결정이 갈리면 옵션 나열 대신 **추천안 하나 제시 후 진행**, 막히면 짧게 묻는다.
- 신규 파일/모듈이 늘면 README에 한 줄로 구조를 갱신해 따라잡기 쉽게 둔다.
